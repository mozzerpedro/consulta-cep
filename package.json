{
  "name": "consulta-cep",
  "version": "1.0.0",
  "description": "API simples de consulta de CEP usando o ViaCEP como origem",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test \"test/**/*.test.js\"",
    "test:watch": "node --test --watch \"test/**/*.test.js\""
  },
  "engines": {
    "node": ">=20.12"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "express": "^4.19.2",
    "express-rate-limit": "^8.7.0",
    "redis": "^6.2.1"
  }
}
